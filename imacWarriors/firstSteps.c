#include <stdio.h>
#include <stdlib.h>

struct unit {
    int x; 
    int y; // x and y = coordinates in the game
    
    char type[2];
    type[0] = w; // w for warrior
    type[1] = s; // s for servant
    
    
} Unit;

struct world {
    
}